-- =============================================================================
-- Операционный день заведения: во сколько утра начинаются "новые сутки".
-- Бронь на 00:00-05:59 раньше трактовалась как полночь ТЕКУЩЕЙ календарной даты —
-- для заведений, работающих допоздна, это "поздний вечер вчера", не "начало нового
-- дня", и авто-«опаздывает» ложно срабатывало сразу после полуночи (юзер-фидбек 2026-07-22).
-- Идемпотентно. Применять в Supabase → SQL Editor.
-- =============================================================================

ALTER TABLE restaurant_settings
  ADD COLUMN IF NOT EXISTS day_start_hour int NOT NULL DEFAULT 6;
