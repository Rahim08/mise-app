-- =============================================================================
-- mise News — приоритет/важность поста. Аддитивно, idempotent.
-- Уровни: normal (обычная), important (важная), urgent (срочная). Важные/срочные
-- подсвечиваются и закрепляются сверху ленты. Ось «приоритет» независима от «типа».
-- Применять в Supabase → SQL Editor (после news-2026-06.sql).
-- =============================================================================

ALTER TABLE news_posts
  ADD COLUMN IF NOT EXISTS priority text NOT NULL DEFAULT 'normal';
