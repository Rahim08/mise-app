-- =============================================================================
-- Analytics → вкладка «Прогноз/Цели»: цель выручки на месяц.
-- Хранится в restaurant_settings (одно значение, владелец правит во вкладке).
-- Аддитивно, idempotent. Применять в Supabase → SQL Editor.
-- Код graceful: без этой колонки вкладка «Прогноз» работает (прогноз по темпу),
-- просто блок «Цель» не сохраняется до применения миграции.
-- =============================================================================

ALTER TABLE restaurant_settings
  ADD COLUMN IF NOT EXISTS monthly_revenue_goal numeric NOT NULL DEFAULT 0;
