# Ревью 2026-07-17: аудиты, дашборд, уведомления, скорость

Статусы: [ ] не начато · [x] сделано. Приоритет: P1 багфикс · P2 UX/логика · P3 полировка/идеи.
Это продолжение AUDIT-2026-07-17.md (34 находки — все закрыты, см. FIXPLAN). Здесь — новый проход
по логике/удобству/текстам после добавления аудит-чек-листов.

## Что уже проверено и починено (сессия 17.07)

- Ошибки: `npx tsc` чистый, `npm run build` OK, iOS BUILD SUCCEEDED. Всё закоммичено (77e-коммит «fix(audit)»).
- Зависание: PDF-отчёт рендерится вне main thread (было — замораживал UI); анимация «e» через TimelineView (было — «бегущие слова» после logout).
- Скорость/данные: кеш iOS сбрасывается на pull-to-refresh; `DB.fresh()` для read-modify-write; чек-листы больше не теряют чужие отметки (мерж по свежей копии, офлайн-очередь и для INSERT).
- НЕ проверено вживую: прогон iOS на устройстве, мобильный нижний бар дашборда в реальном браузере. Нужен ручной смоук-тест.

## А. Баги (P1)

- [x] **A1. Разовые/повторяющиеся аудиты (веб): отметка попадает в СТАРЫЙ прогон.**
  `app/people/page.tsx` AdHocAuditsView: `completions` грузятся БЕЗ фильтра по дате (`select('*')`),
  `setItem` ищет `completions.find(c => c.checklist_id === audit.id)` — для повторяющегося аудита
  найдётся вчерашний/произвольный прогон, сегодняшний тоггл перезапишет его. iOS фильтрует по
  `todayKey` — веб привести к тому же: грузить `eq('date', today)` (или последний прогон по дате)
  и искать по `(checklist_id, date)`.
- [x] **A2. Статистика аудитов считает «нарушениями» незаполненные прогоны.**
  AuditStatsView: каждый неотмеченный пункт любого completion (включая `status='pending'` —
  только что заведённые кроном/менеджером и ещё не открытые) идёт в «Топ нарушений» и роняет
  «% выполнения». Пустой pending-прогон = 100% нарушений. Считать только по завершённым
  (`status='done'`) или хотя бы `in_progress`+`done`; pending показывать отдельно как «не пройдено».
- [x] **A3. Веб-«Разовые»: нет редактирования/удаления шаблона аудита.** iOS даёт pencil/trash,
  веб — только создание. Ошибся в пункте — шаблон навсегда + каждый «запуск» создаёт НОВЫЙ шаблон
  (плодятся дубли в списке). Добавить edit/delete + повторный запуск существующего шаблона
  (как iOS `startAudit`), а не создание нового.
- [x] **A4. Дашборд-уведомления не показывают журнал `notifications`.**
  `dashboard/notifications/page.tsx` — только заказы QR-меню + low-stock + past_due. Явка,
  аудиты, кассы, нарушения, напоминания — владелец на вебе их не видит вообще (видит только
  пуш на телефоне). Добавить ленту из `notifications` (то же перерендеривание по
  `title_key`/`body_key`, образец — колокольчик в people/page.tsx:907).
- [x] **A5. Настройки напоминаний обещают то, чего cron не делает.**
  Settings → «Напоминание о смене»: режимы «за N часов»/«в фиксированное время» сохраняются,
  но cron (Vercel Hobby, 1 запуск/день) их игнорирует — шлёт всегда накануне. UI вводит в
  заблуждение. Либо скрыть режимы до апгрейда на Pro, либо подпись «работает после апгрейда»
  (комментарий в коде это признаёт, UI — нет).

## Б. Аудиты: до уровня SafetyCulture/iAuditor (P2)

Что уже есть и работает как у конкурентов: шаблоны, разовые запуски, расписание
(daily/weekly/monthly), адресация (точка/цех/сотрудник), обязательное фото, гео-гейт,
нарушение → задача с фото и пушем, статистика 30 дней, рейтинг сотрудников, пресеты (санитария).

Чего не хватает до «инспекторского» класса (в порядке ценности):

- [x] **Б1. Статусы пункта: ✓ / ✗ / N/A вместо бинарного «отмечен».** Сейчас «не отметил» =
  «нарушение» — нельзя отличить «не проверял» от «проверил, плохо». У всех шести конкурентов
  пункт имеет исход pass/fail/na. Схема: `items_state[i].result: 'pass'|'fail'|'na'` (done
  оставить для обратной совместимости). Это же чинит смысл статистики (А2).
  [18.07: grading-режим ТОЛЬКО для разовых аудитов (чек-листы смены остаются бинарными).
  Веб: ChecklistCard prop `grading`, пилюли ✓/✗/N/A; result⇒done=true («проверен»), повторный
  тап снимает; pass с обязательным фото — прежний фото-гейт; fail сразу открывает форму
  «нарушение → задача». iOS: ChecklistRunCard.gradingRow, gradeAuditItem/persistAuditItem
  (общий мерж-код с toggle), офлайн-очередь несёт newState целиком. Статистика на обеих
  платформах: N/A вне знаменателя, fail = нарушение, legacy done = pass.]
- [x] **Б2. Комментарий к пункту.** Фейл без пояснения бесполезен для разбора. Поле
  `items_state[i].note` + textarea в карточке (веб и iOS).
  [18.07: веб — textarea по иконке, сохранение по blur; iOS — ItemNoteSheet (TextEditor,
  detent 280). Ключ pe.itemNote на 8 языках в обоих словарях. Комментарий виден под пунктом.]
- [x] **Б3. Отчёт по прогону (PDF/шеринг).** Прогон завершён → одна кнопка «Отчёт»: шапка
  (точка, дата, кто), пункты со статусами, фото, комментарии. Инфраструктура PDF уже есть
  (iOS PDFSnapshot/renderPDF, веб pdfTable) — переиспользовать.
  [18.07: веб AuditReportSheet (счёт %, теги OK/Нарушение/N/A/Не проверено, note, фото-превью)
  + exportAuditRunPdf (jsPDF + PT Sans, фото embed c fallback на URL). iOS AuditRunReportView
  + renderAuditPdf (UIGraphicsPDFRenderer, светлая палитра, рендер в Task.detached, фото
  качаются заранее), share через UIActivityViewController — паттерн ReportExportView.]
- [x] **Б4. История прогонов аудита.** Сейчас виден только сегодняшний прогон; вчерашние
  результаты недоступны ни менеджеру, ни владельцу. Экран «история»: список прогонов по датам
  со статусом и % — вход в отчёт Б3.
  [18.07: кнопка-часы на карточке аудита (менеджер). Веб AuditHistorySheet — запрос 30 дней
  по checklist_id (limit 100); iOS AuditHistorySheet — из m.auditRuns (уже 30 дней). Строка:
  дата · кто · % (pending = «Не проверено»); тап открывает отчёт Б3. runScore/auditRunScore:
  N/A вне знаменателя. Ключи pe.auditHistory/auditHistoryEmpty/auditReport/resultFail/notChecked.]
- [x] **Б5. Дедлайн и ответственный у задачи-нарушения.** createViolationTask создаёт задачу
  без срока. У MaintainX/SafetyCulture corrective action имеет due date. Поле срока + просрочка
  краснеет в задачах. [17.07: веб-нарушение получает due_date=завтра; iOS уже ставил due=сегодня. Просрочка на вебе красная+жирная (status!=done && due<today). 18.07: P3-хвост закрыт — iOS строка задачи показывает бейдж срока (calendar + dd.MM), просрочка красным жирным.]
- [ ] **Б6. Балльная оценка (скоринг).** % выполнения уже есть — добавить веса пунктов позже,
  только если попросят клиенты. Не приоритет.

## В. Дашборд (P2)

- [ ] **В1. Смоук-тест нового мобильного таб-бара.** Пилюли заменены нижним баром + шит «Ещё»
  (аудит-находка 24, код готов). Проверить на телефоне: safe-area, перекрытие контента,
  скролл шита, тёмная тема, бейдж.
- [ ] **В2. Бейдж уведомлений постоянно горит при low-stock.** Осознанное решение (склад
  требует действия), но проверить, не раздражает ли: возможно low-stock показывать точкой
  другого цвета или гасить до изменения списка позиций.
- [x] **В3. Аудиты не видны владельцу на дашборде.** Весь функционал аудитов живёт в
  /people (staff-вход). Владелец в dashboard-shell видит People-embedded — проверить, что
  вкладка «Аудиты» доступна и удобна из shell, и что статистика аудитов есть в Обзоре
  (хотя бы % за 30 дней + топ нарушений).
  [18.07: Обзор — glance-карточка «Аудиты · 30 дней»: % выполнения (цвет по порогам 80/50)
  + топ-3 нарушений, клик ведёт в /dashboard/people; появляется только когда есть
  завершённые прогоны, гейт appOk('people'). Логика счёта = AuditStatsView (result-aware,
  N/A вне знаменателя). Путь в аудиты из shell: People → «Зал» → «Чек-листы» — доступен;
  удобство пути проверить в ручном смоуке вместе с В1.]
- [x] **В4. Единообразие пустых состояний.** У ленты уведомлений есть дружелюбное «тихо, пока
  нет уведомлений», у аудитов — сухое «Нет аудитов». Пройтись по пустым состояниям разделов
  shell: короткая подсказка «что это и как начать» + кнопка действия. [17.07: пустое состояние «Разовых» аудитов — подсказка менеджеру (pe.noAuditsHint, 8 языков); 18.07: веб — подсказки у Уведомлений (pe.noNotifsHint) и Дисциплины (pe.disEmptyHint); iOS — подсказки у Задач, Отчётов, Уведомлений, Дисциплины, Меню (порт assignTaskHint/reportHint/itemsAddedInDash + 2 новых ключа). Кнопки действия уже есть рядом во всех разделах.]

## Г. Уведомления: тексты и отображение (P2–P3)

Проверено: все cron/событийные пуши идут через dispatchNotification и рендерятся на языке
УСТРОЙСТВА получателя (8 языков); журнал хранит EN + ключи, веб-колокольчик перерендеривает
на языке зрителя. Тексты в notifyStrings.ts ровные, без эмодзи, форматы величин единые.

- [x] **Г1. Заголовок дневного дайджеста закупа = «Общее · Закуп».** После перевода на
  общий шаблон purchaseTitle{category:'general'} заголовок дайджеста неотличим от разового
  закупа категории «Общее». Профессиональнее отдельный ключ `notify.purchaseDigestTitle`
  («Закуп за день» / "Daily purchase summary", 8 языков) — 5 минут работы.
- [x] **Г2. iOS не имеет журнала уведомлений.** Пуш смахнул — информация потеряна; веб-колокольчик
  есть, в приложении нет. Экран «Уведомления» в iOS (чтение notifications + перерендер по
  title_key/body_key — ключи уже в Localization.swift не все: понадобится мапа notifyStrings).
  [18.07: NotifyStrings.swift — порт lib/notifyStrings.ts (все notify.* ключи, 8 языков,
  JSONVal для jsonb-params, notifTitle/notifBody с purchaseTitle-категорией и '_segments').
  Колокольчик с точкой непрочитанного рядом с сегментами вкладки «Смены» (ShiftsHubTab) →
  NotificationsSheet; открытие = read_at (одним update .in), staff видит свои, owner — to_owner.
  Ключи pe.notifications/pe.noNotifs добавлены в Localization.swift. Тексты правятся синхронно
  с lib/notifyStrings.ts — источник правды там.]
- [x] **Г3. Суммы кассы в журнале хранятся EN-рендером.** secure-сегменты (дайджест закрытия
  смены с суммами) не перерендериваются на языке зрителя в колокольчике — только пуш локализован.
  Известное ограничение (комментарий в lib/notify.ts). Донести body_segments до журнала.
  [18.07: конвенция body_key='_segments' + body_params={segments} (jsonb, БЕЗ миграции);
  dispatchNotification пишет сегменты (secure — только получателям с show_cash_amount),
  рендер на языке зрителя: веб people-колокольчик + dashboard/notifications + iOS notifBody.
  Старые записи (EN-рендер в body) показываются как раньше — фолбэк не тронут.]
- [x] **Г4. «{who}{n} позиц.»** — если who передаётся без хвостового пробела/разделителя,
  получится «Иван3 позиц.». Проверить все места отправки purchase (веб/iOS): who должен
  оканчиваться «: » или быть пустым.

## Д. Скорость (P2–P3)

- [x] **Д1. Неограниченные выборки.** `shift_checklist_completions.select('*')` без фильтра
  (AdHocAuditsView — закроется A1) и вообще 31×`select('*')` в people/page.tsx. Пройтись:
  везде, где история растёт (completions, notifications, attendance), добавить `gte('date', …)`
  / `limit`. Иначе через полгода людные точки грузят мегабайты на каждый заход.
  [18.07: закрыт. Растущие без границ были: staff_tasks (200, 17.07), staff_reports (200, веб;
  iOS уже 100), shift_swap_requests (200 обе платформы), staff_tasks iOS (200). Остальные
  ограничены датой (completions eq today / gte 30д, attendance месяц/62, menu_orders 2д+100,
  notifications 50) — не трогал. Справочники (staff_directory, checklists, menu) не растут.]
- [ ] **Д2. people/page.tsx = 2627 строк, PeopleView.swift = 3942.** Не срочно, но каждый
  новый функционал сюда — рост бандла и времени правок. При следующей крупной фиче — разнести
  по файлам (веб: вкладки в components/people/*; iOS: extension-файлы).
- [ ] **Д3. Бейдж дашборда опрашивает 2 таблицы каждые 60 с** — приемлемо; если появятся
  жалобы на квоты Supabase, объединить в один RPC.

## Статус 17.07 (вечер)

Сделано и закоммичено: A1-A5, Г1; Г4 проверен — не баг (оба клиента шлют who с «: »).
Следующий шаг: Б1+Б2 (pass/fail/N/A + комментарий) — трогает items_state на ОБЕИХ платформах,
iOS ChecklistItemState обязан получить новые опциональные поля ДО веб-UI, иначе iOS-тоггл
перезапишет items_state и сотрёт оценки, записанные вебом. Потом Б3 (отчёт), Б4 (история), Б5, Г2, Г3, Д1.

## Статус 18.07

Сделано за день (веб + iOS, детали в чекбоксах): Б1+Б2 (оценки ✓/✗/N/A + комментарий),
Б3 (отчёт прогона + PDF), Б4 (история прогонов), Г2 (журнал уведомлений iOS,
NotifyStrings.swift), Г3 (сегменты в журнале, '_segments'), Д1 (лимиты), P3-хвост Б5
(бейдж срока в iOS-задаче). Блок Б закрыт (кроме Б6 — не приоритет), блок Г закрыт,
блок Д: Д1 [x], Д2/Д3 отложены. tsc чистый, next build OK, iOS BUILD SUCCEEDED.
Закоммичено 18cc19c (Б1-Б4, Г2, Г3, Д1, P3) + В3 отдельным коммитом (карточка аудитов
в Обзоре). НЕ запушено — сперва миграции. Осталось: В1 (ручной смоук мобильного таб-бара
+ путь к аудитам из shell), В2 (бейдж low-stock — понаблюдать), В4-остаток (пустые
состояния), Д2 (разнос файлов), Б6 (скоринг по весам — по запросу клиентов).

## Порядок работ (предложение)

1. P1: A1, A2 (портят данные/смысл статистики уже сейчас) → A3, A4, A5.
2. Б1+Б2 (меняют схему items_state — лучше до накопления данных), затем Б3, Б4, Б5.
3. Г1 (быстро), В1 (смоук), остальное по мере.

Проверки после правок — как в FIXPLAN: tsc + build + xcodebuild; не пушить без применённых миграций.
