# План фиксов по аудиту AUDIT-2026-07-17.md

Статусы: [ ] не начато · [x] сделано · [~] частично. Обновлять по ходу.
Номера находок — из AUDIT-2026-07-17.md (там же file:line и рецепты).

## Решения пользователя (2026-07-17, подтверждены)

1. **Зарплата (13):** канон = iOS-модель. iOS НЕ трогать. Веб привести к iOS: вычитать авансы (salary_advances), карта строго из monthly_card_amounts БЕЗ fallback на employees.card_amount, авто-прогулы (source='auto') НЕ удерживать.
2. **salary_advances.date:** тип уточнит позже/даст доступ. Фикс `ym+"-31"` → key(monthEnd) делать в любом случае (безопасен при обоих типах).
3. **Таймзона (7):** делать правильно — колонка restaurant_settings.timezone + учёт в cron.
4. **Роль admin (5):** даёт права менеджера на ОБЕИХ платформах (isManager/canSeeMoney/isOfficial).
5. **Mise Menu сотрудникам (6):** НЕ выдавать — убрать 'menu' из APPS в components/dash/shared.tsx (форма команды). Шлюз не расширять.
6. **late_grace_min (15):** менеджеру МОЖНО менять. Узкое исключение в шлюзе (см. рецепт ниже).
7. **stock_left (23):** оставить ручным, авто-декремент НЕ делать (не пользуются). Skip.
8. **Elite/Ledger (27):** добавить кнопку добавления в корзину в строки (как в Lounge).
9. **Имена модулей (28):** SKIP по решению.
10. **Stripe-дедуп (9):** делать. Таблица stripe_events, код терпит отсутствие таблицы.

Деплой-порядок: `menu qr-features migration.sql` применяет пользователь в Supabase ДО git push (находка 3). Новые миграции этого плана — тоже пользователь.

## FABLE (сложное: деньги/гонки/шлюз/конкурентность)

- [x] **20. Шлюз:** update вычищает scope-колонку из values — app/api/db/route.ts (op==='update': delete values[scope]).
- [x] **15. late_grace_min менеджеру:** исключение в шлюзе (restaurant_settings + update + values ⊆ {late_grace_min} → разрешить people-write) + веб проверяет error и откатывает (app/people/page.tsx saveGrace ~2263).
- [x] **1. persistInkassation** insert-перед-delete по образцу persistExpenses — ManagerView.swift:285-294.
- [x] **2+18. iOS MenuOrder:** table_number: Int?→String? (Models.swift:175) + рендер; isCall = call != nil, поддержка coal/water (PeopleView.swift:3663 + рендер вызова, локализованные подписи pe.qaCoal/pe.qaWater есть? — если нет ключей, добавить в Localization.swift).
- [x] **13. Веб-зарплата → iOS-модель:** SalaryTab (people) + analytics/page.tsx: cardOf без fallback, advanceOf + 7-й запрос salary_advances, exportSalary/PDF и тоталы/строки renderSalary по формуле iOS (total=max(0,salary−deduct), cash=max(0,total−advance−card)), аванс показан в раскрытии строки.
- [x] **14. ym+"-31" → key(monthEnd):** PeopleView.swift:295, AnalyticsView.swift:464.
- [x] **12. Кеш vs pull-to-refresh:** DB.swift — добавлен `CacheStore.clearAllNow()` (awaitable); AppTabPage.refreshable (MainView.swift:204) и ManagerView.refreshable (:486) чистят кеш ПЕРЕД load. Оффлайн-кеш сохранён.
- [x] **10 (Stripe-дедуп, решение 10):** webhook вставляет event_id в stripe_events до обработки; 23505 → received:true; иная ошибка (нет таблицы) → лог и работа без дедупа. SQL: docs/migrations/stripe-events-2026-07.sql (применяет пользователь).
- [x] **7. Таймзона cron:** миграция docs/migrations/restaurant-timezone-2026-07.sql; reminders/route.ts: localParts(now,tz)/nextDayKey/loadTimezones (select('*') терпит отсутствие колонки); напоминания/no-show/аудиты/close-reminder считают «сегодня/завтра/HH:MM/dow/dom» в tz точки (запросы расширены на соседние даты + фильтр). НЕ сделано: UI выбора tz в Настройках (отдать Sonnet).
- [x] **16. Чек-листы lost-update + очередь:** DB.swift: добавлен `.fresh()` (чтение мимо кеша, без stale-fallback); оба тоггла перечитывают прогон fresh и мержат ТОЛЬКО свой индекс; INSERT-путь при офлайне кладёт в очередь (PendingChecklistToggle: completionId? | checklistId+date+shiftId); flush читает fresh, создаёт прогон если офлайн-INSERT так и не прошёл. Веб: оба setItem (чек-листы+аудиты в people/page.tsx) перечитывают строку и мержат свой индекс.
- [x] **19. PDF с main thread:** ReportExportView: PDFSnapshot (@unchecked Sendable, все t()/Money.s посчитаны на MainActor в pdfSnapshot()) + nonisolated static renderPDF(snapshot); generate() зовёт через Task.detached. AppModel.swift:81 (DEBUG demo-путь) обновлён.
- [x] **32+31. «Бегущие слова» + бесшовность:** Brand.swift FlowingGradientText — TimelineView(.animation), фаза = f(timeline.date) % 6s (нет наслоения repeatForever после logout); палитра g+g+[g[0]], сдвиг на один период (1.5w при ширине 3w).

## SONNET 5 (механика, рецепты в AUDIT-2026-07-17.md по номерам)

- [x] **4 (решение 4). Роль admin = менеджер:** app/people/page.tsx:2461 isManager += role==='admin'; app/tobacco/page.tsx:386 canSeeMoney += 'admin'; iOS AppModel.swift:40-49 canSeeMoney/isOfficial += "admin"; grep обоих реп на `role === 'manager'` / `role == "manager"` — добавить admin ВЕЗДЕ где гейт менеджера. [сделано Fable в этой сессии]
- [x] **5 (решение 5).** Убрать пункт menu из APPS (components/dash/shared.tsx:13) — сотрудникам не выдаётся; сам объект APPS используется и для карточек — проверить, что карточка «Mise Menu» для владельца (dashboard) не пропала там, где нужна. [сделано Fable: в форме доступов исключён 'menu' через GRANTABLE_APPS, карточки владельца не тронуты]
- [x] **Аудит-находка 3.** «menu qr-features migration.sql» перенесён в docs/migrations/menu-qr-features-2026-07.sql. НЕ применён (пользователь).
- [x] **Аудит 4.** Cron: shift reminders + trial_ending + purchase-дайджест через dispatchNotification; ключи notify.shiftReminderTitle/Body/BodyTime + trialEndingTitle/Body (8 языков); дайджест — notify.purchaseTitle{category:'general'}/purchasePositionsBody + data.digest (bypass глушения daily-push в notify.ts).
- [x] **Аудит 8.** /api/menu/import: staff-токен принимается только с owner=true; PIN-сессии отклоняются.
- [x] **Аудит 10.** Глифы → SVG (people ✓×2, tobacco ✕/✓, bookings ★→SVG-звёзды) и SF Symbols star/star.fill (GoogleReviewsView).
- [x] **Аудит 11.** legacySecrets/SUPABASE_SERVICE_ROLE_KEY-fallback удалён из lib/staffToken.ts (TTL-окно ротации закрыто 14.07).
- [x] **Аудит 17.** iOS PeopleView: checkOut() (update check_out_at + check_out_lat/lng при включённой геоявке, без проверки зоны) + кнопка «Я ушёл» / строка «Ушёл в {t}»; ключи pe.iLeft/pe.checkedOut/pe.leftAt.
- [x] **Аудит 21+34.** Сабтайтлы хаба уже рендерились через t("mod.*.sub") — мёртвые RU-строки удалены из MiseModule; Face ID reasons → t("faceid.loginReason"/"faceid.enableReason"), ключи в Localization.swift (8 языков).
- [x] **Аудит 22.** restaurant_settings.read += 'stash'.
- [x] **Аудит 24.** Mobile dashboard: пилюли заменены нижним фикс-баром (Обзор, Смены, Menu, Брони, Ещё) + bottom-sheet «Ещё» (остальные модули + сервис + аккаунт, бейдж на уведомлениях); TabIcon 'more', ключ dash.navMore; контент получил padding-bottom под бар.
- [x] **Аудит 25.** Бейдж = новые заказы + low-stock (порог как в ленте: min_quantity_g ?? 100); комментарий обновлён.
- [x] **Аудит 26.** applyImport: дедуп по (category_id, name) case-insensitive против localItems — повторный клик/дубли выгрузки не плодят копии.
- [x] **Аудит 27 (решение 8).** altLayouts: Apple/Market уважают settings.show_photos; Elite/Ledger — кнопка «+» (qty-бейдж) при allow_orders && orderable.
- [x] **Аудит 29.** fmtHours через ключи pe.hUnit/pe.mUnit (tr прокинут); monthLabel — локаль tr('dash.locale').
- [x] **Аудит 30.** checkStaffPlanLimit: при update реально затронутые строки считаются select id с теми же фильтрами (не только id eq).
- [x] **Аудит 33.** MainView.save() зеркалит Bool-префы в UserDefaults "mise_notif_prefs" (только Bool — читатели кастят [String: Bool]).
- [x] **7 (хвост).** UI выбора timezone в Настройках (карточка гео/напоминаний): select из Intl.supportedValuesOf('timeZone'), дефолт — tz устройства владельца; ключ dash.timezone.
- [x] **Аудит 31** — сделан Fable вместе с 32 (палитра g+g+[g[0]] в Brand.swift).

## Проверка после правок

- Веб: `npx tsc --noEmit` и `npm run build`.
- iOS: `cd native/Mise && xcodebuild -scheme Mise -sdk iphonesimulator -destination 'generic/platform=iOS Simulator' build` → BUILD SUCCEEDED.
- НЕ коммитить без явной команды пользователя. Прод: git push = деплой.
- Перед деплоем кода меню: пользователь применяет menu qr-features migration.sql (+ новые миграции из этого плана).
